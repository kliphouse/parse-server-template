'use strict';

module.exports = {
    // Environments

    ENV: {
        LOCAL: 'LOCAL',
        DEV: 'DEV',
        QA: 'QA',
        STAGE: 'STAGE',
        PROD: 'PROD'
    }
};

/* Set these variables in ENV
ADMIN_PASSWORD
ADMIN_USERNAME
APP_ID
APP_NAME
CLIENT_KEY
CLOUD_CODE_MAIN
DATABASE_URI || DATABASE_URL || MONGODB_URI
FILE_KEY
MASTER_KEY
NODE_ENV
SERVER_URL
PARSE_MOUNT (Optional) - Defaults to '/parse'
PORT (Optional) - Defaults to 1337
*/